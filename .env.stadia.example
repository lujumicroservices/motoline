# Copy to .env.stadia (gitignored) and paste the Stadia API key.
# Never put this key in apps/mobile/.env — it must stay on the Edge Function.
VALHALLA_URL=https://api.stadiamaps.com/route/v1
VALHALLA_API_KEY=
