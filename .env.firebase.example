# Firebase Admin JSON for FCM send (notify-rodada-invite).
# Copy the downloaded service-account file to .env.firebase.json (gitignored)
# or paste the JSON as FIREBASE_SERVICE_ACCOUNT in a gitignored env file.
# Never put this in the Flutter app or in chat.
