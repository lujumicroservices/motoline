name: ride_core
description: Pure Dart domain for RiderLab — track helpers, scoring, sync outbox.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
